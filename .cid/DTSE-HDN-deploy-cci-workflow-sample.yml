version: 1.0
name: deploy-cci-workflow-sample
language: java


#  构建工具
dependencies:
  base:
    jdk: best
    maven: best
    
#  构建机器
machine:
  standard:
    euler:
      - default
    
#  构建脚本
scripts:
  - mvn versions:set -DnewVersion=${CID_BUILD_TIME} package -Dmaven.test.skip=true
  - ls -al target
#   - docker build -t registry-cbu.huawei.com/dtse-hdn/deploy-cci-workflow-sample:${CID_SHORT_UID} .
  
#  构建产物
artifacts:
  fd:
    deploy-cci-workflow-sample: deploy-cci-workflow-sample-$build_number.tar.gz
