version: 1.0
name: deploy-cci-workflow-sample
language: java


#  构建工具
dependencies:
  base:
    jdk: best
    maven: best
    
#  构建机器
machine:
  standard:
    euler:
      - default
    
#  构建脚本
scripts:
  - mvn package -Dmaven.test.skip=true
  - tar -zcvf  deploy-cci-workflow-sample-${CID_BUILD_NUMBER}.tar.gz  target/
  - ls -al .
#   - docker build -t registry-cbu.huawei.com/dtse-hdn/deploy-cci-workflow-sample:${CID_SHORT_UID} .
  
#  构建产物
artifacts:
  fd:
    deploy-cci-workflow-sample: deploy-cci-workflow-sample-${CID_BUILD_NUMBER}.tar.gz
