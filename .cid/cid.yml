version: '1.0'
name: deploy-cci-workflow-sample
language: java
dependencies:
  base:
    jdk: best
    maven: best
artifacts:
  package:
    - deploy-cci-workflow-sample-${CID_BUILD_NUMBER}.*.tar.gz
machine:
 standard:
    euler:
      - default
scripts:
  - mvn versions:set -DnewVersion=${CID_BUILD_TIME} package -Dmaven.test.skip=true
  - tar -zcvf  deploy-cci-workflow-sample-${CID_BUILD_NUMBER}.tar.gz  target/
  - ls -al .
#   - >-
#     docker build -t
#    registry-cbu.huawei.com/dtse-hdn/deploy-cci-workflow-sample:1.0_${CID_BUILD_NUMBER} .
